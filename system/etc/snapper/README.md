# Requires sudo to capture. On source machine run:
#   sudo cp /etc/snapper/configs/root /etc/snapper/configs/home system/etc/snapper/configs/
# install.sh only enables snapper timers; create configs with `snapper -c root create-config /` if missing.
